# Work Log - DS Capture

## 2026-05-30 (Sat)

### [v1.00.40] - Build at 16:57:58
- 신규 빌드 및 기능 최적화


### [v1.00.38] - Build at 16:46:02
- 신규 빌드 및 기능 최적화

### [v1.00.37] - Build at 16:40:07
- 신규 빌드 및 기능 최적화


## 2026-05-15 (Fri)

### [v1.00.36] - Build at 20:10:56
- 형광펜/일반 펜 불투명도 간섭 버그 수정\n- 사각형/원 그리기 시 Shift 키로 정비율(정사각형/정원) 고정 기능 추가\n- 툴바 레이아웃 최적화 (불투명도 조절 칸 및 구분선 위치 조정)\n- 툴바 구분선 색상을 아이콘 색상과 동기화\n- 실행 시 이미지 크기에 따른 초기 줌 자동 최적화 (해상도 초과 시 화면 맞춤)


### [v1.00.35] - Build at 19:52:32
- 이미지 편집기 고도화(도구 재배치, 화살표/사각형 품질 개선, 저장 확인창) - 메인 UI 버그 수정(Esc 종료 방지, 포커스 안정화, 썸네일 실시간 동기화) - 우클릭 파일 위치 열기 추가

### [v1.00.35] - 이미지 편집기 고도화 및 UI/UX 디테일 완성
- **이미지 편집기 도구 모음 전면 재배치**
    - **논리적 그룹화**: [저장/복사] - [실행 취소] - [그리기] - [변형] 순서로 버튼을 재배치하여 작업 흐름의 일관성 확보.
- **드로잉 엔진 품질 및 로직 고도화**
    - **화살표 시인성 개선**: 화살표 머리(삼각형) 내부를 선 색상으로 채우는 로직을 구현하여 더 명확한 지시선 표현 가능.
    - **사각형 각진 모서리 처리**: `MiterJoin` 및 `SquareCap` 설정을 통해 선 두께와 관계없이 날카로운 직각 모서리를 유지하도록 수정.
    - **경계면 잔상 방지(NoPen)**: 선 색상과 면 색상이 동일할 경우 테두리를 그리지 않도록 최적화하여 깔끔한 단색 도형 렌더링 구현.
- **이미지 편집기 UX 및 안전성 강화**
    - **스마트 저장 확인창**: 그리기, 자르기, 회전 등 실제 이미지 수정이 발생한 경우에만 종료 시 저장 여부를 묻는 컨펌창 노출. (보기 기능인 줌은 제외)
    - **색상 자동 동기화**: "채우기 사용" 체크 시 현재 선택된 선 색상을 채우기 색상으로 즉시 매칭하는 편의 기능 추가.
    - **반응성 개선**: 툴팁 표시 지연 시간을 0.3초로 단축하여 도구 설명 확인 속도 향상.
- **메인 윈도우 및 최근 목록 편의성 확장**
    - **Esc 동작 최적화**: 메인 윈도우에서 Esc 키 입력 시 창이 닫히지 않도록 수정하여 작업 중 실수로 프로그램이 종료되는 현상 방지.
    - **환경설정 포커스 안정화**: 저장 위치 지정 후 환경설정 창이 메인 창 뒤로 숨는 버그를 수정하고 포커스 유지 로직 강화.
    - **실시간 썸네일 동기화**: 저장 위치 변경 즉시 최근 캡처 목록을 해당 폴더의 파일들로 실시간 갱신하도록 개선.
    - **파일 위치 열기 메뉴**: 썸네일 우클릭 시 탐색기를 통해 해당 파일의 위치를 바로 열고 파일을 선택(Select)해주는 기능 추가.

## 2026-05-15 (Fri)
### [v1.00.34] - UI/UX 고도화 및 기업 브랜딩 완성
- **사이드바 고정형 반응형 레이아웃 도입**
    - **고정 폭 사이드바**: 좌측 메뉴의 너비를 고정하여 시각적 안정성을 확보하고, 우측의 최근 캡처 목록 패널이 창 크기에 따라 유동적으로 확장되도록 구조 개편.
    - **지연 로딩(Lazy Loading) 그리드**: 최대 100개의 캡처 기록을 한 번에 불러오지 않고, 스크롤 위치에 따라 10개씩 증분 로드하여 초기 실행 속도와 메모리 사용량 최적화.
- **전문가용 단축키 관리 시스템 구축**
    - **실시간 키 캡처(Key Capture)**: 기존 콤보박스 선택 방식에서 탈피하여, 입력창에 포커스를 두고 키를 누르면 즉시 인식되는 직관적인 설정 방식 도입.
    - **조합키 강제 및 블랙리스트**: 시스템 전역 키와의 충돌을 방지하기 위해 `Ctrl` 또는 `Alt` 조합을 강제하고, `Alt+F4`, `Alt+Tab` 등 위험한 키 조합을 블랙리스트로 차단.
- **저장 워크플로우 다각화**
    - **저장 방식 선택 옵션**: 환경설정에서 "파일 저장 + 클립보드" 또는 "클립보드 전용" 모드를 선택할 수 있는 기능을 추가하여, 단순 복사 업무 시 디스크 용량 낭비 방지.
- **기업 브랜딩 및 디자인 고도화**
    - **로고 및 카피라이트 통합**: 사내 공식 로고를 하단 푸터 영역에 배치하고, 전문적인 카피라이트 문구를 추가하여 인하우스 툴의 완성도 극대화.
    - **환경설정 UI 리디자인**: 설정 항목 증가에 따라 팝업 창 레이아웃을 재조정하고 하이라이트 토글 버튼으로 상태 가독성 향상.
- **배포 파이프라인 안정화**
    - **리소스 자동 패키징**: `build.py` 수정으로 빌드 시 로고 및 아이콘 등 필수 자산이 누락되지 않도록 자동 포함 로직 강화.
    - **버전 관리 자동화**: 빌드 실행 시마다 버전 번호와 날짜/시간을 자동으로 갱신하고 ZIP 압축까지 완료하는 원클릭 프로세스 완성.


## 2026-05-14 (Thu)
### [v1.00.25] - 이미지 편집기 내비게이션 고도화 및 배포 방식 전환
- **배포 아키텍처 전환 (One-file → One-dir)**
    - **초기 로딩 속도 극대화**: PyInstaller의 단일 파일 압축 해제 병목 현상을 해결하기 위해 폴더 기반(`--onedir`) 배포로 전환. 클릭 즉시 프로그램이 실행되는 성능 확보.
    - **자동 배포 자동화**: `build.py`에 빌드 완료 후 결과 폴더를 자동으로 ZIP 압축하는 기능을 추가하여 배포 편의성 증대.
- **이미지 편집기 (`ImageEditor`) UI/UX 완성**
    - **Glide Pan (Space-Glide)**: Space 키를 누른 채 마우스를 움직이기만 하면 클릭 없이도 화면이 이동하는 전문가급 내비게이션 모드 구현.
    - **Overscroll (가상 여백)**: 이미지 경계 밖으로 이동 범위를 확장하여, 이미지의 모서리 부분을 화면 중앙에 두고 정밀하게 편집할 수 있도록 개선.
    - **Floating Fit Button**: 툴바의 복잡도를 낮추기 위해 '화면 맞춤(Zoom Extents)' 기능을 우측 상단 플로팅 버튼으로 분리 배치.
    - **정밀 제어**: 마우스 포인터를 기준으로 하는 앵커 줌(Mouse-Anchored Zoom) 및 커스텀 스핀박스(`CustomSpinBox`) 도입으로 위젯 렌더링 결함 해결.
- **단축키 시스템 보강**: 화면 맞춤(`F`), 실행 취소/재실행 등 주요 기능에 대한 전역 단축키 매핑 완료.

## 2026-05-12 (Tue)
### [v1.00.24] - 서브프로세스 기반 메모리 격리 및 드래그 성능 최적화
- **서브프로세스 기반 메모리 관리 아키텍처 도입**
    - **편집기 분리**: `ImageEditor` 클래스를 독립 스크립트(`DS_image_editor.py`)로 완전 분리.
    - **멀티 엔진 모드**: 단일 EXE 파일이 인자(`--editor`)에 따라 메인 앱 또는 편집기로 동작하는 기술 구현.
    - **RAM 100% 반환**: 편집기를 별도 프로세스로 띄움으로써, 편집기 종료 시 OS가 해당 메모리를 즉시 완벽하게 회수하도록 최적화.
- **드래그 캡처 성능 극대화**
    - **Input Lag 제거**: 자유 드래그 중 실시간 이미지 크롭 및 변환 로직을 제거하여, 마우스 커서와 가이드라인이 100% 동기화되도록 성능 최적화.
- **프로젝트 구조 및 빌드 시스템 고도화**
    - **자산 관리**: `DS_capture.ico`, `DS_capture.png`를 전용 `icon/` 폴더로 이동하여 프로젝트 구조화.
    - **빌드 속도 개선**: `build.py` 수정으로 테스트 빌드(`test`) 시에는 `--onedir` 방식을 사용하여 빌드 시간을 2배 이상 단축.
    - **코드 리팩토링**: `main_entry()` 통합 및 중복 경로 변수 제거로 유지보수성 향상.
- **Git 환경 최적화**: `.gitignore` 보강 및 빌드 임시 파일 저장소 정화 완료.


## 2026-05-11 (Mon)
### [v1.00.22] - Esc 키 동작 수정 및 배포 빌드
- **환경설정 창 Esc 종료 문제 수정**
    - `MainApp._on_esc_main`: Toplevel 창(`winfo_exists`, `winfo_viewable`)이 하나라도 활성화되어 있으면 메인 창이 닫히지 않도록 보호 로직 추가.
    - `MainApp.open_settings_window`: `pop.bind("<Escape>")` 핸들러에서 `return "break"`를 반환하여 부모 윈도우로의 이벤트 전파를 차단하고 `pop.focus_force()` 추가.
- **하위 윈도우 Esc 핸들링 표준화**
    - `ResizableBox`: `_on_esc_box` 메서드 신설 및 `bind("<Escape>")`를 통해 안전한 종료와 `return "break"` 처리.
    - `MainApp.start_drag`: 드래그 캡처 오버레이(`ov`)에 `_on_esc_ov` 로직을 추가하여 Esc 입력 시 안전하게 종료되도록 수정.
    - `MainApp.popup_shortcut_settings`: 단축키 설정 Toplevel 창에 Esc 바인딩 누락분 추가.
- **배포 빌드 실행**: `build.py`를 실행하여 v1.00.22 단일 실행 파일 빌드 완료 (`dist_production/DS Capture.exe`).

### [v1.00.21] - 아이콘 브랜딩 및 이미지 편집기 고도화
- **아이콘 및 브랜드 통합**
    - **AppUserModelID 강제 설정**: `ctypes`를 통해 `SetCurrentProcessExplicitAppUserModelID`를 호출하여 작업표시줄 아이콘 그룹화 및 고정 아이콘 불일치 문제 해결.
    - **전역 아이콘 적용**: `set_window_icon` 함수를 통해 메인 윈도우, Toplevel, 메세지 박스 등 모든 창에 `DS_capture.ico` 일괄 적용.
    - **트레이 아이콘 최적화**: `pystray` 아이콘 생성 시 이미지의 투명 여백을 자동으로 크롭(`getbbox`)하여 트레이 영역에서 아이콘이 더 크게 보이도록 개선.
- **이미지 편집기 (`ImageEditor`) 기능 강화**
    - **도구 상태 관리**: 형광펜(`highlight`) 도구 선택 시 이전 선 두께와 색상을 저장하고, 다른 도구로 변경 시 원복하는 스마트 전환 로직 구현.
    - **그리기 미리보기 최적화**: 캔버스 배율(`scale`)에 따른 선 굵기 미리보기를 실제 저장될 굵기와 동기화.
    - **UI 개선**: 툴바 레이아웃 최적화 및 폰트 설정(맑은 고딕), 색상 팔레트 시인성 개선.
- **전역 Esc 단축키 도입**: `MainApp`, `ImageEditor`, `ResizableBox` 등 주요 클래스에 `bind("<Escape>")` 적용 및 `_on_esc_main` 기초 설계.
- **빌드 자동화 시스템**: `build.py` 내에 `BUILD_VERSION`, `BUILD_DATE`, `BUILD_TIME`을 소스 코드에 자동 주입하는 regex 로직 구현.

## 2026-05-08 (Fri) ~ 2026-05-11 (Mon)
### [v1.00.15] - 빌드 시스템 전환 및 안정화 (Commit: 84f1338 이후)
- **빌드 도구 전환 (Nuitka → PyInstaller)**
    - Python 3.14 환경에서의 Nuitka 컴파일 불안정성으로 인해 **PyInstaller**로 빌드 시스템 전격 교체.
    - `build.py`: `TEST`(--onedir) 및 `PRODUCTION`(--onefile) 모드 지원 및 빌드 아티팩트 자동 정리 기능 구현.
- **보안 및 실행 안정성 강화**
    - **중복 실행 방지**: Windows Mutex(`CreateMutexW`)를 이용한 `enforce_single_instance` 로직 구현.
    - **HWID 동기화**: C# 기반 라이센스 체커와의 호환성을 위해 HWID 생성 로직 최적화 및 서명 검증 방식 일원화.
- **DPI 고해상도 대응**: `SetProcessDpiAwareness`를 통한 고해상도 모니터 폰트 흐림 현상 해결.
- **프로젝트 구조 정리**: `DS Capture.py` → `DS_capture.py`로 파일명 변경 및 관련 리소스 경로 재설정.

### [v1.00.41] - Build at 00:54:05
- 라이센스 시스템 통합 및 전체 리빌드 적용

### [v1.00.43] - Build at 10:13:09
- 유니폼 폴더 구조(assets) 적용

### [v1.00.44] - Build at 15:08:06
- 정기 배포 빌드

### [v1.00.45] - Build at 15:34:01
- 정기 배포 빌드
